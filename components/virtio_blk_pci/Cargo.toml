[package]
name = "virtio_blk_pci"
version = "0.1.0"
edition.workspace = true
authors = ["Axvisor Team"]
description = "virtio-blk PCI device emulation for UEFI guest boot"
license = "Apache-2.0"

[dependencies]
log = "0.4"
ax-errno = { workspace = true }
axaddrspace = { workspace = true }
axdevice_base = { workspace = true }
pci_host = { path = "../pci_host" }
spin = "0.10"
