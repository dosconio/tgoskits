[package]
name = "pci_host"
version = "0.1.0"
edition.workspace = true
authors = ["Axvisor Team"]
description = "PCI Host Bridge emulation for UEFI guest boot"
license = "Apache-2.0"

[dependencies]
log = "0.4"
ax-errno = { workspace = true }
axaddrspace = { workspace = true }
axdevice_base = { workspace = true }
spin = "0.10"