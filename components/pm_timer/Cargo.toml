[package]
name = "pm_timer"
version = "0.1.0"
edition.workspace = true
authors = ["Axvisor Team"]
description = "ACPI Power Management Timer emulation"
license = "Apache-2.0"

[dependencies]
log = "0.4"
ax-errno = { workspace = true }
axaddrspace = { workspace = true }
axdevice_base = { workspace = true }